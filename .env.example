# Development Configuration
# Copy this to .env for local development

# Application Mode (central or agent)
LOGTRAIL_MODE=central

# Server Configuration
NODE_ENV=development
PORT=3000

# Security (for central server)
COOKIE_SECRET=dev-cookie-secret-change-in-production

# Development specific settings
LOG_LEVEL=debug

# For agent development (uncomment when testing agent mode)
# LOGTRAIL_MODE=agent
# AGENT_ID=dev-agent-001
# AGENT_NAME=Development Agent
# AGENT_DESCRIPTION=Local development agent
# CENTRAL_SERVER_URL=ws://localhost:3000
# AGENT_API_KEY=dev-agent-key-change-in-production
