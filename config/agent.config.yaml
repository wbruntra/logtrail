central_url: "https://logtrail2.lunaparkdigital.com"
agent_secret: "some-long-random-string"   # must match AGENT_SECRET on central
app_name: "production_server"                  # label shown in the UI
files:
  - /home/william/logs/redesign-api.log
