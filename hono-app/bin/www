#!/usr/bin/env bun

const app = require('../app')

const port = parseInt(process.env.PORT || '12070', 10)

const server = Bun.serve({
  port,
  fetch: app.fetch,
  idleTimeout: 0,
})

console.log(`Hono server listening on port ${server.port}`)
